#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Created on Sat Jul 23 15:02:46 2022

@author: qli
"""

#abs_path = os.path.abspath('')
#user_id = os.path.expanduser('~').split('/')[-1]
#print(user_id)

# if os.path.isfile('POTCAR'):

# f = [x[0] for x in os.walk('.')]

# f = next(os.walk('.'))[1]


# f = [x[0] for x in os.walk('.')]

# folders = next(os.walk('.'))[1]
# for f in folders:
#     subprocess.call(['cp INCAR KPOINTS run.sh POTCAR ' + f], shell=True)
